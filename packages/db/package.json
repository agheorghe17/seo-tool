{
  "name": "db",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./src/index.ts",
    "./schema": "./src/schema/index.ts"
  },
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "generate": "pnpm build && drizzle-kit generate",
    "migrate": "drizzle-kit migrate",
    "policies": "tsx src/apply-policies.ts",
    "push": "pnpm build && drizzle-kit push",
    "studio": "pnpm build && drizzle-kit studio",
    "lint": "eslint src",
    "type-check": "tsc --noEmit"
  },
  "dependencies": {
    "drizzle-orm": "^0.38.3",
    "postgres": "^3.4.5",
    "shared": "workspace:*"
  },
  "devDependencies": {
    "config": "workspace:*",
    "drizzle-kit": "^0.30.1",
    "tsx": "^4.19.2",
    "typescript": "^5.7.3"
  }
}
